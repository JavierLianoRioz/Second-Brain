# Misión: Implementación de CrewAI para el Second Brain

## Estado Actual
- Alerta sobre `900 Bin` recibida: la carpeta es volátil.
- Cambio de estrategia: reubicación a `Crew/` en la raíz.

## TODO
- [ ] Crear carpeta `Crew/config` en la raíz.
- [ ] Generar boilerplate de `agents.yaml` y `tasks.yaml` en `Crew/config/`.
- [ ] Implementar `crew.py` en `Crew/`.
- [ ] Crear `main.py` en `Crew/`.
- [ ] Añadir `requirements.txt` en `Crew/`.

## Notas de Diseño
- Estabilidad: `Crew/` será el centro operativo persistente.
- Los agentes se definirán progresivamente según las necesidades del usuario.
